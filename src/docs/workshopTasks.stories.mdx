import { Meta } from "@storybook/addon-docs";

<Meta title="Docs/Workshop tasks" />

# Workshop tasks

1. Divide `src/views/Applayout` into components. Create atleast one new component.
2. Create story or multiple stories for new component(s)
   - Try default properties
   - Make each variant different by properties
3. Test you changes on react app. 👉 `src/App.tsx`
4. Create error handling for your component
